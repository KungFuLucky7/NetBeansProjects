package interpreter.debugger.UI;

import interpreter.debugger.DebugVirtualMachine;

/**
 * Continue: Continue execution
 * 
 * @author Terry Wong
 */
public class Continue extends RunCommand {

    @Override
    public void execute(DebugVirtualMachine vm) {
    }
}
